% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync_packages.R
\name{sync_packages}
\alias{sync_packages}
\title{Sync per-chapter package lists from the eda4ml book repo}
\usage{
sync_packages(book_path = NULL, book_chapters = NULL)
}
\arguments{
\item{book_path}{Character. Path to the local `eda4ml` book repo. If
NULL, defaults to a sibling directory `../eda4ml`.}

\item{book_chapters}{Character. Path to `book-chapters.txt`. If NULL,
defaults to the file shipped with `eda4mlr`.}
}
\value{
A tibble with columns `chapter` (integer) and `pkg` (character).
}
\description{
Parses each chapter QMD in the local `eda4ml` checkout and returns a
tibble of chapter-package pairs faithful to what each chapter loads.
Reads only the labeled `CRAN-libraries` chunk per chapter; flags any
`local-libraries` chunk that loads something other than `eda4mlr`.
}
\keyword{internal}
